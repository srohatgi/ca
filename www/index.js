function onIndexLoad() {
  //alert('calling http://maps.googleapis.com/maps/api/js?sensor=true&callback=getGeolocation')
  if (isConnected) {
    // load folders and activities
    
  } else {
    alert("Must be connected to the Internet");
  }
}
